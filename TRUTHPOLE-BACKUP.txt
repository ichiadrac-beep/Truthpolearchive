TRUTHPOLE — The Archive
Full workspace restore backup
Created 20260825-030725 UTC

This zip is the project tree: source, public assets (including the
transparent alien trademark PNG), configs, scripts, and startup.

Restore:
  1. Unzip into an empty project folder
  2. npm install
  3. npm run dev
     (or: sh startup.sh)

Includes: src, public, server, scripts, migrations, package.json,
package-lock.json, vite/tsconfig, startup.sh, trademark logo.

Excludes: node_modules, build output, caches, screenshots, nested zips.
